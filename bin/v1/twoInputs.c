#include <stdio.h>
#include <stdint.h>

int main() {
    uint64_t x = 0;
    uint64_t y = 0;
    uint64_t z = 0;

    scanf("%u", &x);
    scanf("%u", &y);


    if (x > 10) {
        printf("The result is %u", z);
    }

    return 0;
}
