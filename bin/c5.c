#include <stdio.h>
#include <stdint.h>

/*----- Component 5 -----*/

int main() {
    uint64_t recipient = 0;
    uint64_t dest;
    uint64_t input;


    scanf("%u", &dest);
    if (dest == 5)

        scanf("%u", &input);

        if (input == 14) {
            printf("%u%u\n", recipient, input);
        }

    return 0;
}